<h1 class="my-4">Categorias</h1>
<div class="list-group">
  <% @categories.each do |category|%>
    <%= link_to category.description, site_category_path(category), class: "list-group-item" %></a>
  <% end %>
</div>